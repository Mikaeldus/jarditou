console.log("TEST");

    /***
     * Exemple d'utilisation des methodes d'ajout/suppression d'un element dans le tableau
  
    var array = ['Mikael', 'Clement'];

    array.push('John') //Ajoute John a la fin du tableau
    array.unshift('John') // Ajoute JOhn au debut du tableau
    array.shift() // Retire ('Mikael') le premier element du tableau
    array.pop() // Retire ('Clement') le dernier Element du tableau

   */